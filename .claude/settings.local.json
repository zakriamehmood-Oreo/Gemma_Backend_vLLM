{
  "permissions": {
    "allow": [
      "Bash(pip --version)",
      "Bash(python -c \"import torch; print\\('torch:', torch.__version__\\); print\\('CUDA:', torch.cuda.is_available\\(\\)\\)\")"
    ]
  }
}
