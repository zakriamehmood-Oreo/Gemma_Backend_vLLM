# Core API
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.4
pydantic-settings==2.9.1

# Monitoring
prometheus-client==0.21.1

# Transformers backend (local dev + fallback)
torch==2.11.0
transformers==5.7.0
accelerate==1.6.0
bitsandbytes==0.45.5
huggingface-hub==1.13.0
sentencepiece==0.2.0
protobuf>=3.20.0

# vLLM backend (production/Linux+GPU only)
# Uncomment on the cloud instance — do NOT install on Windows
# vllm>=0.4.0
